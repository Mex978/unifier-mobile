// import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  // initModule(MangaChapterModule());
  // MangaChapterController mangachapter;
  //
  setUp(() {
    //     mangachapter = MangaChapterModule.to.get<MangaChapterController>();
  });

  group('MangaChapterController Test', () {
    //   test("First Test", () {
    //     expect(mangachapter, isInstanceOf<MangaChapterController>());
    //   });

    //   test("Set Value", () {
    //     expect(mangachapter.value, equals(0));
    //     mangachapter.increment();
    //     expect(mangachapter.value, equals(1));
    //   });
  });
}
