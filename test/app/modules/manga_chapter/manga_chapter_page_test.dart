import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MangaChapterPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MangaChapterPage(title: 'MangaChapter')));
    //  final titleFinder = find.text('MangaChapter');
    //  expect(titleFinder, findsOneWidget);
  });
}
