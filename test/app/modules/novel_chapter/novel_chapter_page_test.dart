import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('NovelChapterPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NovelChapterPage(title: 'NovelChapter')));
    //  final titleFinder = find.text('NovelChapter');
    //  expect(titleFinder, findsOneWidget);
  });
}
