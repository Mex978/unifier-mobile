import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ChaptersPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ChaptersPage(title: 'Chapters')));
    //  final titleFinder = find.text('Chapters');
    //  expect(titleFinder, findsOneWidget);
  });
}
